import 'package:flutter/material.dart';

void main() {
  runApp(const CloudlyApp());
}

class CloudlyApp extends StatelessWidget {
  const CloudlyApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
